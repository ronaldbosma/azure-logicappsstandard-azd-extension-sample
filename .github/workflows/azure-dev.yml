name: azure-dev

on:
  workflow_dispatch:
    inputs:
      cleanup-resources:
        description: "Clean up resources after deployment"
        required: false
        default: true
        type: boolean
      logicappsstandard-extension-id-or-url:
        description: "ID or URL of the azure.logicappsstandard azd extension to use"
        required: false
        default: "https://azuresdkartifacts.z5.web.core.windows.net/azd/extensions/pr/10058/azure-logicappsstandard.zip"
        type: string
  pull_request:
    # Also trigger on ready_for_review to catch the transition from draft to ready state.
    types: [opened, synchronize, reopened, ready_for_review]
    branches:
      - main
    paths:
      - ".github/workflows/azure-dev.yml"
      - "infra/**"
      - "src/**"
      - "tests/IntegrationTests/**"
      - "azure.yaml"
      - "bicepconfig.json"

defaults:
  run:
    shell: pwsh # Use PowerShell Core for all scripts (the azd hooks are written in PowerShell)

env:
  # Add a unique suffix to the environment name for pull requests to avoid name conflicts
  AZURE_ENV_NAME: ${{ github.event.pull_request.number && format('{0}-pr{1}', vars.AZURE_ENV_NAME, github.event.pull_request.number) || vars.AZURE_ENV_NAME }}
  AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}
  AZURE_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID || vars.AZURE_SUBSCRIPTION_ID }}
  # Configure the ID/URL of the azure.logicappsstandard azd extension to use
  # By using a variable, we can default to the standard extension but override this with e.g. a prerelease build if needed.
  AZURE_LOGICAPPSSTANDARD_EXTENSION_ID_OR_URL: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.logicappsstandard-extension-id-or-url || 'https://azuresdkartifacts.z5.web.core.windows.net/azd/extensions/pr/10058/azure-logicappsstandard.zip' }}

jobs:
  # ------------------------------------------------------------
  # Build, Verify and Package
  # ------------------------------------------------------------

  build-verify-package:
    name: Build, Verify and Package
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Required to fetch an OIDC token for Azure authentication
      contents: read # Required to checkout code if needed

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup azd
        uses: Azure/setup-azd@v2

      - name: Setup azure.logicappsstandard azd extension
        run: |
          azd ext install "$env:AZURE_LOGICAPPSSTANDARD_EXTENSION_ID_OR_URL"

      - name: Setup .NET 10
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"

      - name: Print Tool Versions
        run: |
          az version
          az bicep version
          azd version
          azd ext list
          Write-Host ".NET SDK Version: $(dotnet --version)"

      # Use Azure CLI authentication with azd commands so credentials are shared between azd commands and az (Azure CLI) commands used in hooks.
      - name: Configure azd to use Azure CLI Authentication
        run: |
          azd config set auth.useAzCliAuth "true"

      # Login to the Azure CLI with OpenID Connect (OIDC) using federated identity credentials.
      - name: Azure CLI Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID || vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID || vars.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID || vars.AZURE_SUBSCRIPTION_ID }}

      - name: Bicep Lint
        run: |
          az bicep lint --file ./infra/main.bicepparam

      - name: Validate Template
        run: |
          az deployment sub validate `
            --template-file './infra/main.bicep' `
            --location $env:AZURE_LOCATION `
            --parameters environmentName=$env:AZURE_ENV_NAME `
                         location=$env:AZURE_LOCATION

      - name: Create artifacts folder
        run: |
          mkdir -p ./artifacts

      - name: Package Logic App Without Code
        run: |
          azd package logicAppWithoutCode --output-path ./artifacts/logicAppWithoutCode-package.zip --no-prompt

      - name: Package Logic App With Code
        run: |
          azd package logicAppWithCode --output-path ./artifacts/logicAppWithCode-package.zip --no-prompt

      - name: Build Integration Tests
        run: |
          dotnet build ./tests/IntegrationTests/IntegrationTests.csproj --configuration Release --output ./artifacts/integration-tests

      - name: Upload Logic App Without Code Package
        uses: actions/upload-artifact@v7
        with:
          name: logicAppWithoutCode-package
          path: ./artifacts/logicAppWithoutCode-package.zip
          retention-days: 1

      - name: Upload Logic App With Code Package
        uses: actions/upload-artifact@v7
        with:
          name: logicAppWithCode-package
          path: ./artifacts/logicAppWithCode-package.zip
          retention-days: 1

      - name: Upload Integration Tests Package
        uses: actions/upload-artifact@v7
        with:
          name: integration-tests-package
          path: ./artifacts/integration-tests/
          retention-days: 1

  # ------------------------------------------------------------
  # Deploy to Azure
  # ------------------------------------------------------------

  deploy:
    name: Deploy to Azure
    needs: build-verify-package
    # Only deploy if it's not a pull request or if the pull request is ready for review (not a draft) to avoid deploying from work-in-progress branches.
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Required to fetch an OIDC token for Azure authentication
      contents: read # Required to checkout code if needed

    outputs:
      AZURE_RESOURCE_GROUP: ${{ steps.get-outputs.outputs.AZURE_RESOURCE_GROUP }}
      AZURE_LOGIC_APP_WITHOUT_CODE_NAME: ${{ steps.get-outputs.outputs.AZURE_LOGIC_APP_WITHOUT_CODE_NAME }}
      AZURE_LOGIC_APP_WITH_CODE_NAME: ${{ steps.get-outputs.outputs.AZURE_LOGIC_APP_WITH_CODE_NAME }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup azd
        uses: Azure/setup-azd@v2

      - name: Setup azure.logicappsstandard azd extension
        run: |
          azd ext install "$env:AZURE_LOGICAPPSSTANDARD_EXTENSION_ID_OR_URL"

      # Use Azure CLI authentication with azd commands so credentials are shared between azd commands and az (Azure CLI) commands used in hooks.
      - name: Configure azd to use Azure CLI Authentication
        run: |
          azd config set auth.useAzCliAuth "true"

      # Login to the Azure CLI with OpenID Connect (OIDC) using federated identity credentials.
      - name: Azure CLI Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID || vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID || vars.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID || vars.AZURE_SUBSCRIPTION_ID }}

      - name: Provision Infrastructure
        run: |
          azd provision --no-prompt

      - name: Download Logic App Without Code Package
        uses: actions/download-artifact@v8
        with:
          name: logicAppWithoutCode-package
          path: ./artifacts

      - name: Deploy Logic App Without Code
        run: |
          azd deploy logicAppWithoutCode --from-package ./artifacts/logicAppWithoutCode-package.zip --no-prompt

      - name: Download Logic App With Code Package
        uses: actions/download-artifact@v8
        with:
          name: logicAppWithCode-package
          path: ./artifacts

      - name: Deploy Logic App With Code
        run: |
          azd deploy logicAppWithCode --from-package ./artifacts/logicAppWithCode-package.zip --no-prompt

      # Extract relevant azd environment variables and make them available as job outputs for subsequent jobs.
      - name: Get Output Variables
        id: get-outputs
        run: |
          $variableNames = @(
            "AZURE_RESOURCE_GROUP",
            "AZURE_LOGIC_APP_WITHOUT_CODE_NAME",
            "AZURE_LOGIC_APP_WITH_CODE_NAME"
          )
          .\.github\workflows\scripts\export-azd-env-variables.ps1 -VariableNames $variableNames

  # ------------------------------------------------------------
  # Verify Deployment
  # ------------------------------------------------------------

  verify-deployment:
    name: Verify Deployment
    needs: deploy
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Required to fetch an OIDC token for Azure authentication

    steps:
      - name: Setup .NET 10
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: "10.0.x"

      - name: Download Integration Tests Package
        uses: actions/download-artifact@v8
        with:
          name: integration-tests-package
          path: ./artifacts/integration-tests

      # Login to the Azure CLI with OpenID Connect (OIDC) using federated identity credentials.
      # This is necessary for the integration test to access Azure resources if needed.
      - name: Azure CLI Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID || vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID || vars.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID || vars.AZURE_SUBSCRIPTION_ID }}

      - name: Run Integration Tests
        run: |
          dotnet ./artifacts/integration-tests/IntegrationTests.dll --report-trx --results-directory ./artifacts/integration-tests/TestResults
        working-directory: ./
        env:
          # Pass the necessary deployed resource properties as environment variables so the integration tests can access them.
          AZURE_TENANT_ID: ${{ secrets.AZURE_TENANT_ID || vars.AZURE_TENANT_ID }}
          AZURE_RESOURCE_GROUP: ${{ needs.deploy.outputs.AZURE_RESOURCE_GROUP }}
          AZURE_LOGIC_APP_WITHOUT_CODE_NAME: ${{ needs.deploy.outputs.AZURE_LOGIC_APP_WITHOUT_CODE_NAME }}
          AZURE_LOGIC_APP_WITH_CODE_NAME: ${{ needs.deploy.outputs.AZURE_LOGIC_APP_WITH_CODE_NAME }}

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: integration-test-results
          path: ./artifacts/integration-tests/TestResults/
          retention-days: 1

  # ------------------------------------------------------------
  # Clean Up Resources
  # ------------------------------------------------------------

  cleanup:
    name: Clean Up Resources
    needs: [deploy, verify-deployment]
    if: ${{ success() && (github.event_name != 'workflow_dispatch' || github.event.inputs.cleanup-resources == 'true') }}
    runs-on: ubuntu-latest
    permissions:
      id-token: write # Required to fetch an OIDC token for Azure authentication
      contents: read # Required to checkout code if needed

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup azd
        uses: Azure/setup-azd@v2

      - name: Setup azure.logicappsstandard azd extension
        run: |
          azd ext install "$env:AZURE_LOGICAPPSSTANDARD_EXTENSION_ID_OR_URL"

      # Use Azure CLI authentication with azd commands so credentials are shared between azd commands and az (Azure CLI) commands used in hooks.
      - name: Configure azd to use Azure CLI Authentication
        run: |
          azd config set auth.useAzCliAuth "true"

      # Login to the Azure CLI with OpenID Connect (OIDC) using federated identity credentials.
      - name: Azure CLI Login
        uses: azure/login@v3
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID || vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID || vars.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID || vars.AZURE_SUBSCRIPTION_ID }}

      # Clean up using the --purge flag to ensure all resources are permanently deleted, like API Management.
      - name: Cleanup Resources
        run: |
          azd down --purge --force --no-prompt
